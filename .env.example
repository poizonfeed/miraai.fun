# Destination for the primary CTA button. The site reads this at build time.
# Replace with your real referral or start-in-Telegram link, then `npm run dev` or rebuild.
VITE_MIRA_CTA_URL=https://t.me/MiraAIBot
